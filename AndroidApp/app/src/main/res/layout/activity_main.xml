<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hotel_surface"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/hotel_deep_blue"
        android:paddingHorizontal="@dimen/spacing_md"
        android:theme="@style/ThemeOverlay.Material3.Dark.ActionBar"
        app:title="@string/main_title"
        app:titleTextColor="@color/hotel_white" />

    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/spacing_md"
        android:background="@drawable/room_card_background"
        android:iconifiedByDefault="false"
        android:queryHint="@string/search_hint" />

    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/hotel_white"
        app:itemIconTint="@color/hotel_deep_blue"
        app:itemTextColor="@color/hotel_deep_blue"
        app:menu="@menu/menu_bottom_nav" />

</LinearLayout>
